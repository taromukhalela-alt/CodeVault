/*
 * MIDDLEWARE AGGREGATOR
 * Purpose: Central export file for all middleware functions
 * Connected To: server.js (imports middleware), routes (applies route-specific middleware)
 * Used By: Server configuration for setting up global and route middleware
 * Returns: Object containing all middleware functions
 */
