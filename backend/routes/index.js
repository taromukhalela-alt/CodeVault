/*
 * ROUTE AGGREGATOR
 * Purpose: Central file that imports and registers all API routes
 * Connected To: server.js (imported to attach routes to Express app)
 * Used By: server.js for setting up route middleware
 * Returns: Router configuration with all endpoints
 */
