/*
 * BACKEND CONSTANTS
 * Purpose: Centralized constants used throughout the backend (error codes, messages, statuses)
 * Connected To: Controllers, services, middleware, error handlers
 * Used By: Throughout backend for consistent constant values
 * Contains: API response codes, error messages, status codes, validation constants
 */
