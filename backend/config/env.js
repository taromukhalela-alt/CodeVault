/*
 * ENVIRONMENT CONFIGURATION
 * Purpose: Loads and validates environment variables from .env file
 * Connected To: server.js (imported at app startup), all modules that need env vars
 * Used By: Any module requiring API keys, ports, database URLs, etc.
 * Returns: Validated environment variables object
 */
