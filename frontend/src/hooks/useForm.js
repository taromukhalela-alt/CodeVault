/*
 * USE FORM HOOK
 * Purpose: Custom hook for form state management and validation
 * Connected To: components/auth/LoginForm.jsx, components/auth/RegisterForm.jsx,
 *               Profile.jsx and other form-based components
 * Used By: Form components for handling form state, validation, and submission
 * Returns: { values, errors, touched, handleChange, handleBlur, handleSubmit, setFieldValue }
 */
