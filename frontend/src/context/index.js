/*
 * CONTEXT AGGREGATOR
 * Purpose: Central export file for all context providers
 * Connected To: App.jsx (imports and uses all contexts), pages and components that use context
 * Used By: App.jsx for setting up providers, components for consuming context
 * Returns: Object containing all exported contexts
 */
