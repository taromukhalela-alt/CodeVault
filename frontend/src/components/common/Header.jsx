/*
 * HEADER COMPONENT
 * Purpose: Main navigation and header component displayed on all pages
 * Connected To: pages/ (used in layout/shell), context/AuthContext.js (accesses user auth state)
 * Used By: Page layouts, App.jsx for consistent header across application
 * Props: navigation items, user info, logout handler
 * Styling: App.css, index.css
 */
