/*
 * PROTECTED ROUTE COMPONENT
 * Purpose: HOC/wrapper to protect routes that require authentication
 * Connected To: App.jsx (used in routing), context/AuthContext.js (checks auth status)
 * Used By: App routing to redirect unauthenticated users to login
 * Process: Checks if user is authenticated, allows access or redirects to login
 * Styling: N/A
 */
