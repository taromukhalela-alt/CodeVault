/*
 * MAIN APP ENTRY POINT
 * Purpose: Main React application entry component that sets up routing, context, and layout
 * Connected To: main.jsx (imported by main.jsx), context providers, all pages and components
 * Used By: main.jsx for rendering the entire React application
 * Responsibilities: Set up routing, provide contexts, define page layout
 */
