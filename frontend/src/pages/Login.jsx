/*
 * LOGIN PAGE
 * Purpose: User authentication page
 * Connected To: App.jsx (routing), components/auth/LoginForm.jsx (login form),
 *               context/AuthContext.js (auth state), services/authService.js
 * Used By: App routing for /login path, unauthenticated users
 * Displays: Login form, forgot password link, register link
 * Styling: App.css, index.css
 */
