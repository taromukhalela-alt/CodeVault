/*
 * DASHBOARD PAGE
 * Purpose: Main user dashboard (protected route)
 * Connected To: App.jsx (routing), components/ProtectedRoute.jsx (protection),
 *               context/AuthContext.js (user data), services/
 * Used By: App routing for /dashboard path, authenticated users
 * Displays: User stats, navigation to features, user profile quick access
 * Styling: App.css, index.css
 */
