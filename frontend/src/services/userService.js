/*
 * USER SERVICE
 * Purpose: API calls for user operations (fetch profile, update profile, etc.)
 * Connected To: context/AuthContext.js, pages/Profile.jsx, pages/Dashboard.jsx
 * Used By: Pages and components that need user data and operations
 * Functions: getProfile(), updateProfile(), getUsers(), deleteAccount(), etc.
 * Base URL: Configured in config or environment variables
 */
