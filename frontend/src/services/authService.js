/*
 * AUTHENTICATION SERVICE
 * Purpose: API calls for authentication (login, register, logout, token refresh)
 * Connected To: context/AuthContext.js (auth state management), pages/Login.jsx, pages/Register.jsx,
 *               components/auth/LoginForm.jsx, components/auth/RegisterForm.jsx
 * Used By: Authentication pages and forms for backend communication
 * Functions: login(), register(), logout(), refreshToken(), getCurrentUser(), etc.
 * Base URL: Configured in config or environment variables
 */
