/*
 * LOCAL STORAGE UTILITIES
 * Purpose: Helper functions for browser localStorage operations
 * Connected To: context/AuthContext.jsx (persists auth tokens), UserContext.jsx
 * Used By: Context providers and components that need persistent storage
 * Functions: setToken(), getToken(), removeToken(), setUserPrefs(), etc.
 */
